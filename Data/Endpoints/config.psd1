@{
    config = @{
        Export = @{
            description = "Export configured items from your Falcon environment"
        }
        Import = @{
            description = "Import exclusions, groups and policies from a 'FalconConfig' archive"
        }
    }
}